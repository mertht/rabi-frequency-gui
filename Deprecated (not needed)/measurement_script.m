function [image] = measurement_script(exposure_time, handles, hardware)
    % measurement_script    executes a sequence of pulses. The pb
    % instructions SHOULD ALREADY BE COMPLETED THROUGH
    % pb_stop_programming(). Once the measurement is finished, the
    % Pulseblaster is stopped and closed.
    
    % exposure_time - exposure time in (ns)
    
    
    % initialize camera measurement
    

end